@page "/"
@using System.Security.Cryptography
@using Pschool.Models.Dtos
@inherits ParentsBase

@if (Parents == null)
{
    <DisplaySpinner/>
}
else
{

    var parentsCount = Parents.Count();

    @if (parentsCount > 0)
    {
        <DisplayParents></DisplayParents>
    }
}



  